def check(current_version: str) -> dict:
    raise NotImplementedError(
        "dumb-drop (dumbwareio/dumbdrop) publishes only 'latest' and git-SHA tags on Docker Hub; "
        "no stable semver tags exist to track."
    )
