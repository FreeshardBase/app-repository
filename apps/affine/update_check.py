def check(current_version: str) -> dict:
    raise NotImplementedError(
        "affine uses ghcr.io/toeverything/affine-graphql:stable (mutable tag); "
        "listing GHCR tags requires packages:read scope not present in the current token."
    )
