networks:
    portal:
        external: true

services:
    trilium-next:
        restart: always
        image: triliumnext/notes:v0.91.6
        container_name: trilium-next
        volumes:
        - "{{ fs.app_data }}/data:/home/node/trilium-data"
        networks:
        - portal
