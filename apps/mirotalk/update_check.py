def check(current_version: str) -> dict:
    raise NotImplementedError(
        "mirotalk uses a private image at portalapps.azurecr.io/ptl-apps/mirotalk:master "
        "(branch-tracking tag); no public versioned releases to track."
    )
