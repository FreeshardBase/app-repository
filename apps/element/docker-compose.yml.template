networks:
    portal:
        external: true

services:
    element:
        restart: always
        image: vectorim/element-web:v1.12.7
        container_name: element
        volumes:
        - "{{ fs.app_data }}/data:/data"
        networks:
        - portal
