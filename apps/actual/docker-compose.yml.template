version: '3.5'

networks:
    portal:
        external: true

services:
    actual:
        restart: always
        image: actualbudget/actual-server:24.9.0
        container_name: actual
        volumes:
        - "{{ fs.app_data }}/data:/data"
        networks:
        - portal
